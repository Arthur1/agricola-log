# LogRicola

## composer&npm

```
$ composer install
$ npm install
$ npm run dev
```

## docker

- `docker/nginx/server.conf` を用意してください

```
$ docker-compose build
$ docker-compose up -d
$ docker exec -it agricola-log_php_1 bash
# oil refine migrate
```
